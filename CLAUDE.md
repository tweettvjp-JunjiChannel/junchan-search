# プロジェクト基本ルール & 開発ガイドライン

## 1. 作業完了時の標準通知プロトコル
すべての実装・検証テストが完了したら、人間に指示される前に必ず以下の「2重完了通知」を自発的に実行して作業を終了すること。
1. **PCアラーム音**：Windowsのビープ音（ピピッ、ピピッ）を複数回再生する。
2. **スマホ通知**：以下の `curl` コマンドを使用して、ntfy へ完了通知を送信する。
   日本語メッセージを `curl -d "..."` に直接渡すと、Windows環境ではエンコードが崩れ、
   ntfy側で通知ではなく「ファイル添付」として届いてしまう不具合を実機で確認済みのため、
   文字化け・添付ファイル化を防ぐため必ず以下の書式（ヒアドキュメント＋Content-Type明示）を厳守すること：
   ```
   curl -X POST --data-binary "@-" -H "Content-Type: text/plain; charset=utf-8" https://ntfy.sh/junchan2026 <<< "【作業完了】（ここに行った作業の簡単な要約を記載）"
   ```

## 2. 記事・コンテンツ構造の絶対仕様
- 本文構造：サブスク案内（赤枠）の直下に、元記事確認用の完全なタイトルを表示する「青枠スタイルボックス」を必ず配置すること。
- 一覧概要（スニペット）：WordPress一覧ページの概要欄には、本文抜粋ではなく「note_full_title（95文字切り捨て前のフルタイトル）」をそのまま使用すること。
- 自動同期：`auto_sync_blogs.py` は常に上記（赤枠＋青枠構造、概要のフルタイトル化）を維持して過去記事および新規記事を同期すること。

## 3. 検索優先順位ロジック
- WordPress標準の post_title（95文字制限）だけでなく、カスタムフィールド等の `note_full_title` も検索判定対象に含めること。
- タイトルに検索キーワードが含まれる記事が「絶対的最優先」で上位表示され、本文のみに含まれる記事はその後に並ぶSQL/クエリソート構造を維持すること。

## 4. 目次生成（TOC）ルール
- note.com からの本文取得時は、Playwright の永続セッション（chrome_user_data）を使用し、有料部分を含めた完全な本文・見出しデータを取得して目次（build_toc）を生成すること。

## 5. 記事内リンクの一括変換・バックフィル作業の教訓
（2026-08-09の内部リンク化対応で判明した反省点。今後同種の「本文一括書き換え」作業では必ず踏まえること）

- **ドライランのみで「完了」と報告しない**：`--execute` を付けない実行は本番のデータベース・実データには一切反映されない。作業完了を報告する前に、必ず対象データに対して本番実行（`--execute`等）を行い、実際に反映されたことをAPI等で確認してから完了報告・完了通知を行うこと。ドライラン結果の提示だけで「完了しました」と報告してはならない。
- **`<a href>` だけでなく埋め込み構造も網羅する**：note記事等では、通常のテキストリンク（`<a href>`）以外に、「ブログカード」のような `<figure>`＋`<iframe>` によるoEmbed埋め込みが存在する。iframeはリンク先ドメインのページをそのまま表示するため、`href`だけを書き換えても見た目のカード（iframe内部）をクリックすると変換前のドメインへ遷移してしまう。リンク変換作業では、プレーンリンクと埋め込みブロック（iframe/wp-block-embed等）の両方を洗い出し、実機（Playwright等）でクリック後の遷移先URLまで確認すること。
- **埋め込みカードを内部リンク化する際は、リッチなカードデザイン（アイキャッチ画像＋タイトル＋サイト名）を維持する**：見た目のカードを単純な青文字のテキストリンクに置き換えるだけでは体験が劣化する。WordPress側の対応記事からアイキャッチ画像・タイトルを取得し、元の embedded-service="note" カードに近いカード型UI（インラインstyleで完結する `<a>` ブロック）として再構築すること（`internal_article_links.py` の `build_article_card_html` を参照）。
- **note/exblog記事の判定にWordPressカテゴリーを使わない**：`auto_sync_blogs.py` は現在は新規note記事にも「note」カテゴリー（ID 2471）を正しく設定するが（2026-08-11に修正。詳細は8項目目参照）、内部リンク化の対応記事判定は今後もカテゴリーに依存させないこと。カテゴリー割り当て処理自体に将来また不具合が起きても内部リンク化まで連鎖して壊れないようにするための独立設計であり、必ずスラッグ規則（note記事="n{key}"、exblog記事="exblog-{記事ID}"）で全投稿を走査して行うこと（`internal_article_links.py` の `classify_slug` / `list_target_posts` を参照）。
- **WP REST APIのページネーションは `orderby=id&order=asc` を明示する**：既定の並び順（date DESC）は、一括移行で同一日時に大量作成された投稿のタイ（同着）順序がページ間で安定せず、`page=N` を繰り返すだけで投稿が重複・欠落することが実機で確認された（対応表のサイズは同じでも中身が実行のたびに微妙に違う、という気づきにくい形で発生する）。全件走査系の処理は必ず一意なキー（id）で明示的に order を固定すること。

## 6. 価格バッジ・スキ数の同期に関する仕様（2026-08-09 追記：新規記事が一覧上「無料」に見える事故を受けて）
- 一覧カード・記事上部の価格バッジ／スキ数表示は `note-style-engagement.php` プラグインが担っており、`codoc.jp`への毎回のライブアクセスを避けるため **postmeta のキャッシュ値（`codoc_cached_price` / `codoc_cache_updated_at` / `nseb_like_count`）だけを読む**設計になっている。このキャッシュはWP-Cronで1日2回しか更新されないため、`auto_sync_blogs.py` で新規作成した直後の記事は初回Cron実行までキャッシュが空（`codoc_cache_updated_at=0`）のままとなり、フォールバック用メタキー `codoc_price`（XML一括移行時にのみ設定される、REST非公開の別キー）も未設定なため、一時的に「無料」と誤表示される。
- **対策**：`auto_sync_blogs.py` は新規投稿・更新の両方で、投稿と同時に `codoc_cached_price` と `codoc_cache_updated_at`（現在時刻）を明示的に設定すること（`build_note_sync_meta` を参照）。Cronの実行を待つ設計にしないこと。
- 価格キャッシュを補正する際は、**note.com側の価格をそのまま正としない**こと。90日経過後の自動値下げ（`sync_codoc_discount`）等、WordPress側で意図的に価格を変更しているケースがあるため、正は常に本文中に埋め込まれたCodocブロックの `price`（`extract_codoc_price`）にすること。
- スキ数（`nseb_like_count`）は note.com API の `likeCount` をそのまま正として同期してよい（WordPress側で独自に増減させるロジックは存在しないため）。
- 全記事監査・再同期は `backfill_codoc_price_and_likes.py` を参照。

## 7. 品質保証（QA）ルール（2026-08-09 追記：「507 Insufficient Storage」混入事故を受けて）
- **①出力内容の目視レベルチェック**：スクリプトの正常終了（Exit Code 0）だけで成功と判断しないこと。生成・置換したHTML内に「507 Insufficient Storage」「404 Not Found」「500 Internal Server Error」等の異常なエラー文字列が混入していないか、本番反映前に必ずテキスト検査（文字列検索）を行うこと。
- **②実機検証（Playwright）の高度化**：Playwrightでの検証時は要素の有無だけでなく、(a) タイトルが正常な日本語テキストになっているか、(b) 画像URL（アイキャッチ等）が正しくセットされ200を返すか、(c) エラー文字列が画面上に露出していないか、を人間目線でアサーションすること。
- **③サーバー負荷時のフェイルセーフ**：外部・自サイトを問わずメタデータ取得時に507等の異常なエラー応答を受けた場合、そのエラー文字列を本番コンテンツに埋め込んではならない。リトライするか、取得失敗時は安全なフォールバック（画像なし・タイトルのみ等）で処理を継続すること。特に、ページ表示のたびにライブでメタデータ取得を行うショートコード（Cocoonテーマの`[blogcard url="..."]`等）は、取得先の一時的な障害がそのまま閲覧者に見えるエラー文字列として露出するリスクを常に抱えるため、内部記事へのリンクカードは可能な限りビルド時（バックフィル/同期時）に取得した情報で静的HTMLとして構築し、閲覧のたびのライブ取得に依存しない設計を優先すること（`internal_article_links.py` の `build_article_card_html` / `replace_blogcard_shortcodes` を参照）。

## 8. note記事のカテゴリー割り当てとE2Eテスト（2026-08-11 追記：新規note記事が「Uncategorized」になる不具合を受けて）
- **不具合の経緯**：`auto_sync_blogs.py` の新規note記事投稿処理（`sync_new_note_posts`）が `categories=[1]`（Uncategorized）を固定で設定していたため、日々の自動同期で追加される新しいnote記事が「note」カテゴリー（ID 2471）に入らず、サイドバーの絞り込み検索（`custom-search-filter.php` の `Custom_Search_Category_Filter::CAT_MAP`。`category__in=[2471, ...]` で判定）から検索結果ごと漏れる事故が発生した。`internal_article_links.py` の対応記事判定（スラッグ規則ベース）はこの問題と無関係で影響を受けていない（用途が異なるため意図的にカテゴリーに依存しない設計を維持している。上記5項目のスラッグ規則ルールは変更なしで有効）。
- **恒久対策（実装済み）**：`auto_sync_blogs.py` は新規投稿時に `internal_article_links.NOTE_CATEGORY_ID`（2471）を必ず `categories` に設定する。さらに、WordPress REST APIのレスポンスを直接検証し、万が一カテゴリーが反映されていなければその場で `update_post` により再設定するフォールバックを組み込んである（`sync_new_note_posts` 内、`created.get("categories")` のチェック箇所）。既存記事の更新追従（`sync_note_updates`）でも、更新のたびに対象記事の現在のカテゴリーを検査し、noteカテゴリーが欠けていれば自己修復するようにしてある。**新しい割り当てロジックを追加する際は、必ず「取得・紐付けに失敗してもUncategorizedが露出しない」フォールバックを一緒に設計すること。**
- **既存データの補正**：`backfill_note_category.py` で、スラッグ規則（`n{key}`）に合致するのにnoteカテゴリーが欠けている投稿を一括検出・補正できる（デフォルトはドライラン、`--execute` で本番反映）。同種の「カテゴリー・タクソノミー不整合」が疑われる場合はまずこのスクリプトのドライランで規模を確認すること。
- **E2E（回帰）テストの必須化**：`test_e2e_note_category_filter.py` に、単一ページの要素有無だけを見る「点のテスト」ではなく、(1) note記事の新規同期・更新追従を実際に実行する→(2) REST APIでカテゴリー付与を検証する→(3) 実ブラウザでサイドバーの「note」絞り込み検索フォームを実際に操作する→(4) 検索結果に対象記事がUncategorizedに弾かれず正しく表示されることをアサートする、という一連の「線のテスト」を実装済み。**同期・タクソノミー・検索フィルタに関わる変更を行った場合は、コミット/デプロイ前に必ずこのE2Eテストを実行し、パスすることを確認すること。**
- Windows環境でのコンソール出力について：note記事タイトルには絵文字が含まれることが多く、既定のコンソールエンコーディング（cp932）では `print()` がクラッシュする。`auto_sync_blogs.py` 系のスクリプトはすべて起動時に `sys.stdout.reconfigure(encoding="utf-8")` を行うこと。

## 9. 一覧ページの「いいね」表示同期とbfcache対策（2026-08-11 追記：詳細ページでのいいねが一覧に反映されない不具合を受けて）
- **不具合の経緯**：詳細ページで「いいね」を押した直後、リロードせずブラウザの「戻る」で一覧ページへ戻ると、ハートが白い（未いいね）・カウントが0のままに見える事故が写真で報告された。F5リロードすると正しく反映される。原因はJS未実装や単純なキャッシュではなく、**ブラウザのbfcache（back/forward cache）**：戻る操作でページが「離脱した瞬間のDOMスナップショット」としてそのまま復元され、`DOMContentLoaded` が発火せずスクリプトも再実行されないため、`initCardBadges`/`initStatusBar`（`note-style-engagement.php` の `print_frontend_js`）が一度も再実行されず、訪問前の古いバッジが固まって見えていた。
- **恒久対策（実装済み・v2.0.2）**：`initStatusBar` / `initCardBadges` を「何度呼び出しても安全な冪等関数」として実装し直した（クリックリスナーは `btn.dataset.nsebBound` フラグで一度だけ登録、カードバッジは既存の `.nseb-card-badges` 要素を使い回して中身だけ書き換え、重複追加しない）上で、`window.addEventListener('pageshow', refreshAll)` を追加し、通常の初回ロードだけでなく戻る操作（bfcache復元）でも必ず再実行するようにした。再実行時は (1) まずLocalStorage（`nseb_liked_posts`）を見てハートの見た目だけ即座に書き換え、(2) 続けてバッチAPI（`engage/v1/card-data`。`cache:'no-store'`＋時刻クエリで確実にバイパス）を1回叩いて全記事分の最新値でDOMを再描画する、という2段階で同期する。**新しくページ間の状態同期ロジックを実装する際は、「戻る」操作でbfcacheから復元されるケースを必ず想定し、`pageshow`（`event.persisted`）での再初期化を組み込むこと。単純な`DOMContentLoaded`だけでは戻る操作を拾えない。**
- **N+1対策**：一覧ページの全記事IDを1回のバッチリクエスト（`engage/v1/card-data?ids=...`）にまとめて送る設計は既存のまま維持（`initCardBadges` 参照）。新しく動的データを追加する場合も個別記事ごとのリクエストを増やさないこと。
- **E2Eテストの必須化**：`test_e2e_like_sync_back_navigation.py` に、(1) 一覧の記事カードを特定→(2) 詳細ページで実際に「いいね」を押す→(3) `page.go_back()`（リロード無し）で一覧に戻る→(4) 該当カードのハートが赤く、カウントが詳細ページと完全に一致することをアサート、という線のテストを実装済み。**このテストは本番記事に実際の「いいね」を1件残すため、終了時（成功・失敗を問わず`finally`）に `engage/v1/like/{id}` へ `liked:false` を投げて必ず元の状態に復元する。**同様に本番データへ副作用を残すE2Eテストを新設する場合は、後処理での原状復帰を必ずセットで実装すること。

## 10. 「スキ」の点灯状態とカウント数字の論理整合性（2026-08-12 追記：「赤点灯なのにカウント0」の論理矛盾を受けて）
- **不具合の経緯**：前項（9.）のE2Eテストが本番記事に付けた「いいね」を後処理でカウントだけ0に戻した際、たまたま人間が同じ記事を自分のブラウザで確認していたため、そのブラウザのLocalStorage（`nseb_liked_posts`）には「スキ済み」フラグが残ったまま、DB側のカウントだけ0に戻り、「ハートは赤いのにカウントは0」という論理的に不可能な表示になった。**この設計では「いいね」はDB側の集計カウント（`nseb_like_count`）しか持たず、"誰がいいねしたか"を示すサーバー側の記録は存在しない**（ログイン不要のサイトのため）。したがって、テストや管理操作でカウントだけをリセットしても、それを行った本人以外の実ブラウザのLocalStorageには物理的に手が届かない。**唯一の構造的な解決策は、フロントエンドが「カウント0の記事は絶対に赤点灯にしない」という不変条件を自分自身で常に強制し、矛盾を検知したらLocalStorage側を自動で消す（自己修復する）こと。**新しくLocalStorageベースの状態管理を追加する際は、「サーバー側の値と矛盾したら必ずサーバー側を正として表示側を補正し、LocalStorageも同期して浄化する」という設計を最初から組み込むこと。
- **実装（`note-style-engagement.php` v2.0.3〜）**：`effectiveLiked(postId, count)` が「LocalStorageの"スキ済み"フラグ AND count>0」を判定の一元基準にし、`purgeStaleLikes(ids)` が不整合を検知したLocalStorageエントリを自動で削除する。一覧カードの`renderCardBadges`・詳細ページの`initStatusBar`のいずれも、**サーバーの実カウントが確定した時点**でこの判定を行い自己修復する。ただし、bfcache復元直後の「即座に赤く点灯させる」楽観的先行描画（`paintCardLikeFromCache`）は**意図的にこのcountガードを掛けない**（そこで参照できる「現在表示中の数字」はbfcache復元直後は古い数字であり、真偽の判定に使うと直前に本当にスキした記事まで一瞬「未点灯」に見える回帰バグになることを実機で確認済み）。楽観的な先行描画とサーバー確定後の最終判定は役割が違うので混同しないこと。
- **`pageshow`は`event.persisted`で必ずガードする**：9.項で追加した`pageshow`リスナーを無条件で`refreshAll()`していたところ、通常のフルロードでも`pageshow`が発火するため`ready()`と合わせて2重にfetchCardData〜DOM書き換えが走り、バッジ要素が2回続けて作り直されることで描画タイミングの競合（テストのタイムアウト）を引き起こしていた。`if (event.persisted) { refreshAll(); }` に修正済み。
- **クリック直後のページ離脱でfetchが中断される問題（重要な設計教訓）**：「いいねボタンを押した直後にユーザーがブラウザバック等でページを離れる」と、`fetch()`は`TypeError: Failed to fetch`で例外を投げる。`keepalive: true`を付けると**サーバー側へのリクエスト到達は保証される**（実機で確認：サーバーのカウントは正しく+1される）が、**クライアント側のPromiseは離脱するページ上では結局rejectする**ことがある。この「fetch自体が例外を投げた＝不明な失敗」を「サーバーへの反映に失敗した確定的な失敗」と同列に扱ってLocalStorageをロールバックすると、「サーバー側は+1したのにLocalStorageだけ未いいねに逆戻りする」という**別の新しい不整合を自分で作り出してしまう**。対策として`postLike()`の戻り値を`{ok, confirmed, data}`の形にし、サーバーから明確な失敗応答（4xx/5xx）が返った「確定的な失敗」のみロールバックし、fetch自体の例外（＝離脱による中断の可能性が高い「不明な失敗」）ではロールバックせず、次回ページ表示時の`renderCardBadges`の自己修復ロジックに最終判定を委ねる設計にした。**「サーバーに届いたか分からない」場合に楽観的更新を安易にロールバックしないこと**。自己修復の仕組みが既にあるなら、疑わしきは「そのまま残して次回の認証データ照合に任せる」方が、確実性の低い信号で誤った書き戻しをするより安全。
- **【重要・全E2Eテスト共通の教訓】サーバー側（ConoHa WING/nginx）にページ全体の出力キャッシュがあり、プラグイン更新を本番へアップロード・反映した直後でも、しばらく古いHTML（＝古いインラインJSコード）を返し続けることが実機で確認された（レスポンスヘッダ `X-Nginx-Cache: HIT`）。しかもキャッシュは**URLごとに個別**（トップページ用と各記事の詳細ページ用のキャッシュは別々に古くなりうる）で、この不一致に気づかず「詳細ページは直したのにトップページ用のキャッシュだけ古い」といった**部分的に反応が食い違う状態でデバッグを進めると原因究明が非常に混乱する**（本セッションで実際に数時間ハマった）。**対策**：キャッシュキーはクエリ文字列込みのURLなので、Playwright等でページ遷移する際は必ず一意なクエリ（例：`?_e2e=<timestamp>`）を付けてキャッシュを強制バイパスし、常に最新デプロイのコードを検証すること。プラグイン更新後にE2Eテストで「直したはずなのに直っていない」ように見えたら、まずこのキャッシュ待ち（またはキャッシュバイパス漏れ）を疑うこと。
- **Playwrightでの待機は「セレクタの再クエリ」を使う**：DOM要素が`innerHTML = ''`等で作り直される可能性がある箇所（このプラグインの`renderCardBadges`等）を待つ際、`locator.element_handle()`で一度だけハンドルを取得して`wait_for_function`に渡すと、待機中にその要素が丸ごと差し替えられた場合に「差し替え前の孤立ノード」を見続けてしまいタイムアウトする（本セッションで実際に発生）。`wait_for_function`にはセレクタ文字列を渡し、関数内で`document.querySelector(sel)`のように毎回DOMを再クエリする形にすること。

## 11. PV（閲覧数）カウンターの二重管理と統一（2026-08-12 追記：カード内で👁️バッジとテキストのPV数字が矛盾する不具合を受けて）
- **不具合の経緯**：記事カードの右下👁️バッジ（例：53）と、管理者専用PVパネルのテキスト「全体:」（例：3）が大きく食い違う事故が写真付きで報告された。原因は表示ロジックの単純なバグではなく、**完全に独立した2つのPVカウンターが並存していたこと**：(a) Cocoonテーマ純正のアクセスカウンター（`lib/page-access/access-func.php` の `get_todays_pv()` / `get_last_7days_pv()` / `get_last_30days_pv()` / `get_all_pv()`。管理者・ボットのアクセスを除外して日別に記録し、`tmp/admin-pv.php` が管理者にのみ「本日:/週:/月:/全体:」として表示する）と、(b) `note-style-engagement.php` 独自の `view_count` postmeta カウンター（`POST engage/v1/view/{id}`。管理者・ボットを除外せずカウント）。(b) は開発中の管理者自身のアクセス・E2Eテストのアクセスまで無差別にカウントしていたため、(a) との差が積み上がっていた。
- **恒久対策（実装済み・v2.1.0）**：(b) の独自カウンター（`view_count` postmeta、`POST engage/v1/view/{id}` エンドポイント、`postView()`）を完全に廃止し、(a) のCocoon純正関数を**唯一のデータソース**として直接呼び出す方式に統一した（`get_cocoon_pv_breakdown($post_id)`。ループ外＝REST APIコンテキストから呼ぶため、グローバル`$post`を一時的にセットアップしてから呼び出す必要がある点に注意）。👁️バッジも、管理者PVパネルの「本日/週/月/全体」テキストも、どちらも同じ`engage/v1/card-data`バッチAPIレスポンス（`view_count`=全体, `view_today`, `view_week`, `view_month`）から描画するようJS側 (`syncAdminPvPanel()`) を実装した。Cocoon側のPHPテンプレート（`tmp/admin-pv.php`）自体は変更していない（テーマファイルへの直接変更は将来のテーマ更新で上書きされるリスクがあるため、既存のCSSクラス名（`.today-pv-count`等）を目印にJSから上書きする設計にしている）。**新しくカウンター系の数値を表示する機能を追加する際は、既存の（テーマ・他プラグイン含む）カウンターと重複していないか必ず確認し、重複していれば片方に統一すること。**
- **`.admin-pv`は管理者にのみサーバー側で出力される**：一般訪問者向けの匿名セッション（`curl`や新規プロファイルのPlaywright）ではこのパネル自体がDOMに存在しない。この構造を検証するE2Eテスト（`test_e2e_pv_consistency.py`）は、ログイン済みの永続Chromeプロファイル（`chrome_user_data`。デプロイ作業と共通）を使う必要がある。
- **Cocoonテーマのソースを直接調べる方法**：wp-admin「外観 > テーマファイルエディター」（`theme-editor.php?file=<urlencoded相対パス>&theme=cocoon-master`）で、ログイン済みセッションのPlaywrightから直接ファイル内容を取得できる（`#newcontent` textareaの`input_value()`）。今回はこの方法で `tmp/admin-pv.php` → `lib/page-access/access-func.php` の実装を正確に特定した。Cocoon本体の挙動を推測で実装しないこと。

## 12. PVインクリメント処理がConoHa WING/nginxのページキャッシュで機能停止していた件（2026-08-12 追記：「表示は統一されたが実際に加算されない」不具合を受けて）
- **不具合の経緯**：11.項の対応で👁️バッジとテキストの数字は一致するようになったが、それは「表示の統一」に過ぎず、Cocoon純正のPVカウント処理そのものが機能していなかった。原因はCocoon純正のビーコン方式：記事ページ本体のHTML内にCSS背景画像として`lib/analytics/access.php?post_id=...&t=<PHP側でtime()生成した値>`というGETリクエストのURLが焼き込まれ、これがブラウザに読み込まれることで`logging_page_access()`が呼ばれる仕組みになっている。しかし記事ページ本体がConoHa WING/nginxのページキャッシュに乗ると、そのHTML内に焼き込まれたビーコンURL（`t=`込み）ごとキャッシュされて固定される。このため、キャッシュが再生成されるまでの間、**異なる訪問者が同じキャッシュ済みページを開いても全員が同一のビーコンURLを叩くことになり**、nginxは2回目以降のリクエストを（PHPを実行せず）キャッシュから返してしまう（実機で `X-Nginx-Cache: MISS`→`HIT`→`HIT`…と、2回目以降は`logging_page_access()`が一切実行されなくなることを確認）。
- **紛らわしかった点**：Cocoon純正のカウンターには「同一IP・同一記事・同一日は1回しかカウントしない」という**意図的かつ正しい重複防止仕様**も存在する（`get_accesse_record_from()`で当日の既存レコードを調べ、`last_ip`が前回と同じなら加算しない）。この仕様自体は正しく機能しており、削除・弱体化してはならない。しかし上記のページキャッシュ問題と組み合わさると、「管理者が動作確認のため何度リロードしても増えない」という同じ症状に見えるため、原因の切り分けが非常に紛らわしかった。**PVが増えないという報告を受けたら、まず「同一IP・同日の重複防止（正常）」なのか「キャッシュにより新規訪問者すら記録されない（異常）」なのかを、`GET .../lib/analytics/access.php?...`を同一URLで2回連続リクエストして`X-Nginx-Cache`ヘッダを見る／`POST engage/v1/view/{id}`を直接叩いて`logging_page_access()`の戻り値を見る、といった形で機械的に切り分けること。**
- **恒久対策（実装済み・v2.2.0）**：GETの画像ビーコン方式をやめ、`POST engage/v1/view/{id}`（`handle_view()`）を新設した。POSTはそもそも一般的なキャッシュ層でキャッシュされない上、JS側で`Date.now()`による一意なクエリと`cache:'no-store'`を付けているため、ページ本体がキャッシュされていても確実にサーバーへ到達しPHPが実行される。記録先はCocoon純正の`logging_page_access()`（＝Cocoon純正の`wp_cocoon_accesses`テーブル）をそのまま直接呼び出しており、独自の別カウンターは作っていない（11.項で確立した「単一データソース」の方針を維持）。重複防止ロジックもCocoon側にそのまま委ねているため、Cocoon純正のビーコンとこの新エンドポイントが両方発火しても二重カウントにはならない。**キャッシュ環境下で「必ず1回は実行してほしい副作用のあるリクエスト」を設計する際は、GETの画像/CSSビーコンのような「URLがキャッシュキーになる」方式を避け、POST＋`cache:'no-store'`＋実行のたびに変わるクエリを使うこと。**
- **テスト設計の教訓**：数字同士の静的な比較（「AとBが一致しているか」）だけのE2Eテストでは、「両方とも同じ古いキャッシュ値のまま一致して見える」ケースを検出できない。カウンター系の機能を検証する際は、**必ず「操作前の値を記録する→実際に操作する→操作後に値が動くことをアサートする」という動的なテストを書くこと**（`test_e2e_pv_increment.py`参照）。あわせて、重複防止のような「増えないのが正しい」ケースも同じテストの中で検証し、将来「重複防止が壊れて無限に加算され続ける」という逆方向の回帰も検出できるようにすること。

## 13. Codoc購入数バッジ（🛒）が更新されない・一覧に出ない不具合（2026-08-14 追記）
- **不具合の経緯**：Codocで実際に決済しても詳細ページの購入数バッジ（🛒）が0のまま固定され、さらに一覧カードには価格・PV・スキしかなく購入バッジ自体が出ない、という2件の報告を受けた。原因は2つ：(1) 購入数（`codoc_cached_purchased_count`）はWP-Cronで1日2回（`twicedaily`）しか更新されない設計だったため、決済直後は反映に最大12時間かかっていた。(2) 一覧カードのJS（`renderCardBadges`）が「購入数が1以上の場合のみ」バッジを描画する条件になっており、有料記事でもまだ誰も買っていない（0人）記事は詳細ページには表示があるのに一覧には何も出ない、という矛盾があった。
- **Webhookは存在しない**：「決済完了で即座に連動」を実現するにはCodoc側のWebhookが必要だが、Codocダッシュボード（アカウント設定・関連ページ）をPlaywrightで実機調査した結果、決済完了イベントを能動的に通知する仕組みは存在しないことを確認した（あるのは既存のCMS API用トークン表示のみ）。**この事実は変わっていない前提でCodoc連携を今後拡張する際、Webhookの存在を仮定した設計をしないこと。** 変更があった場合のみ、まず`me/account`等のダッシュボードページをPlaywrightで開いてWebhook/コールバックURLの設定項目が追加されていないか確認してから設計すること。
- **恒久対策（実装済み・v2.3.1）**：
  1. `handle_view()`（単一記事ページを開くたびに叩かれるPVビーコンの受け口）に、その記事1件分だけCodocのbody.json APIをライブ再取得する処理を相乗りさせた（`maybe_live_refresh_codoc_cache_for_post`）。一覧側（`card-data`）は今まで通りキャッシュのみを読み、外部アクセスは一切しない（＝N+1にしない）。詳細ページを開く操作1回で、購入直後でもその記事だけは最新化される。
  2. 連打・アクセス集中対策として、直近`LIVE_REFRESH_MIN_INTERVAL_SECONDS`（300秒）以内に更新済みならライブ再取得をスキップするレート制限を入れた。これは同時にE2Eテストの再現性も支えている：`codoc_cache_updated_at`を「今」にセットしてpostmetaへ直接注入すれば、ライブ再取得によって注入値が実際のCodoc APIレスポンスで上書きされることなく、狙った数値（例:1人）でアサーションできる（`test_e2e_purchase_count_sync.py`参照）。
  3. 全記事の定期更新間隔を`twicedaily`から`hourly`へ短縮した。**既に有効化済みの本番サイトでは`register_activation_hook`は再実行されないため**、`init`フックで`wp_get_schedule()`を見て古い間隔のままなら`wp_clear_scheduled_hook`→`wp_schedule_event`で張り直す自己アップグレード処理を入れた（`maybe_upgrade_cron_schedule`）。cron間隔を変更する際は必ずこの「既存サイトへの反映」を一緒に設計すること（新規インストールしか想定しない`on_activate`だけの変更では本番には効かない）。
  4. 一覧カードの購入バッジは、`purchased_count`が`null`（＝Codocエントリー自体が無い＝無料記事）でない限り、0人でも必ず表示するよう修正し、詳細ページ（`purchased_count===null`のときのみ「-」、それ以外は0でも数字を表示）と仕様を統一した。
- **既存データの補正**：`backfill_codoc_cache.py`で全309件の有料記事（`codoc_entry_code`保持）についてCodoc APIから価格・購入数を再取得しWordPress側へ同期済み（2026-08-14実行、309件成功・0件失敗）。同種の一括同期が必要になった場合はこのスクリプトをそのまま再利用できる。
- **E2Eテスト**：`test_e2e_purchase_count_sync.py`。実在する有料記事1件を選び、`codoc_cached_purchased_count`を一時的に1へ、`codoc_cache_updated_at`を「今」へ書き換えた状態で、(1) 詳細ページに「🛒 1人が購入」、(2) サイト内検索結果の一覧カードに「🛒1」が出ることを確認し、`finally`で必ず元のmeta値へ復元する。**サイト内検索のクエリ生成に注意**：note記事のタイトルはHTMLエンティティ（`&#8230;`等）と絵文字・記号を大量に含むため、素朴な空白区切りの単語分割ではエンティティの残骸（例:「8230」という文字列）が検索語に混入し0件になることを実機で確認した。`html.unescape`してから「英数字・ひらがな・カタカナ・漢字の連続部分」を正規表現で抽出し、6文字以上の最初の1つだけをクエリに使う方式（`clean_search_query`）にすること。

## 14. Codocの公開API purchased_count が実売上を反映しない件、および「購入済み」個人ハイライトの実装（2026-08-14 追記）
- **不具合の経緯**：13.項の対応後も「実際に購入したのに一覧の🛒が0のまま」という報告を受けて再調査した結果、**Codocの公開API（`GET /api/v1/storage/entries/{code}/body.json` の `purchased_count`）自体が実際の売上を反映しないことがある**という、13.項よりさらに根本的な問題を発見した。実例：記事コード`e1BTEzpK9w`はCodocダッシュボードの「売上」ページ（`/me/sales`、要ログイン）に購入番号付き（`P366877-000001`等）で実売上3件が記録されているのに、公開APIの`purchased_count`は3件ともずっと0を返し続けていた。一方、ダッシュボードの記事一覧ページ（`/me/entries`）の各記事行にある🛒（`shopping_basket`）アイコンの数字は、同じ記事で正しく実購入数（1）を表示していた。**教訓：Codocの「購入数」は最低3つの異なる値（公開API・売上ページの取引件数・記事一覧ページのshopping_basketアイコン）を持ちうり、それぞれ更新タイミングやスコープが異なる。今後Codocの数値系データを扱う際は、どのソースを見ているか常に明示し、複数ソースを見比べて食い違いがないか確認すること。**
- **恒久対策1（正しいデータソースへの切替）**：`backfill_codoc_verified_purchases.py`を新設。ログイン済みの永続Chromeプロファイル（`chrome_user_data`）で`/me/entries`をページネーション（`?page=N`、記事行が0件になるまで自動継続）しながら巡回し、各記事行のHTML内にある(1) 公開ページの完全URL（`insert_link`アイコンのhref。例:`https://junchan-world.com/nf66f2dc0c727/`）と(2) `shopping_basket`アイコンの数字を正規表現で抽出する。**Codoc側の記事コードやWordPress側の`codoc_entry_code`を一切介さず、URLのパス（＝スラッグ）だけで直接WordPress記事とマッチングできる**（数字ダッシュボードID⇔英数字entry_code⇔WordPress記事、という3段階の突き合わせが不要になり実装が大幅に単純化した）。`shopping_basket`の数字が0件のときは`<a>`タグで囲まれず素のテキストになる点に正規表現で対応が必要（`(?:<a[^>]*>)?\s*([0-9]+)\s*(?:</a>)?`）。
- **恒久対策2（保護ロジック＝後退防止）**：「プラグイン更新やデプロイで`purchase_count`がリセットされない」という要求に対し、**削除・初期化するコードを書かない**という消極的な保護だけでなく、**既存キャッシュ値より小さい数値では絶対に上書きしない（`max()`を取る）**という積極的な保護ロジックを2箇所に実装した：(1) `refresh_codoc_cache_for_post()`（ライブ更新・cron共通、公開APIを叩く箇所）で`max($api_purchased, $current_purchased)`、(2) `backfill_codoc_verified_purchases.py`側でも更新前に現在値を読み、スクレイピング値が現在値以下なら書き込みをスキップ。公開APIが不正確（本項冒頭）と判明した以上、ライブ更新がダッシュボードスクレイピングで補正した正しい値を後退させないことが特に重要。
- **「購入済み」個人ハイライトの設計（v2.4.0）**：「スキ」と同じくログイン機能が無いサイトのため、サーバー側に「誰が買ったか」の記録は無い（[[section 10と同じ制約]]）。唯一の手がかりは、記事詳細ページのCodoc購入ウィジェット（`.wp-block-codoc-codoc-block`）がそのブラウザに対して購入ボタン（`.codoc-buy-wrap`）を表示するかどうか（実機確認：ウィジェット内に「ログインして購入を復元」の導線があり、購入・購読の権利はCodocログイン状態に紐づく設計）。ボタンが無ければ「単体購入 or 月額サブスク中」とみなし、LocalStorage（`nseb_purchased_posts`）にフラグを記録して一覧・詳細の両方で🛒バッジを赤色ハイライト（`is-purchased`）する（`checkCodocPurchaseState`）。
- **【重大なフォールスポジティブ事故と修正】実装直後の実機テストで、購入したことのない完全に匿名の初回訪問者にまで「購入済み」フラグが立ってしまう致命的なバグを発見した（Playwrightの検証スクリプトで再現・特定。本番に出す前に自前のテストで捕まえられたので実害は出ていない）。原因は、CodocウィジェットのDOM描画が**1回の同期処理ではなく非同期の2段階**（`cms.js`の実行→内部で`body.json`をfetch→約150ms後にレスポンスを反映してDOMを差し替え）で行われており、その間に一瞬だけ「購入ボタンを含まない小さなプレースホルダ」状態が存在すること。当初の実装は「子要素が1つでもあれば判定してよい」という緩い条件で`MutationObserver`のたびに判定していたため、このプレースホルダ状態を「ボタンが無い＝購入済み」と誤検知し、しかも一度立てたフラグを後から取り消すロジックが無かったため、150ms後に本来のロック画面（購入ボタンあり）に差し替わっても誤ったフラグは残り続けた。**修正**：Codocウィジェットの描画完了を示す目印（`.codoc-copyright`。"powered by codoc"のフッター。ロック中・購入済みどちらの最終状態でも必ず描画されることを実機確認済み）が存在するまでは一切判定しないようゲートを追加した。**教訓：サードパーティが非同期に描画するウィジェットの最終状態を「ボタン等の不在」で判定するロジックを書く場合、中間的なローディング状態を必ず疑うこと。DOM要素数や存在チェックだけで「描画完了」を推測せず、そのウィジェットが必ず出力する末尾の要素（フッターやコピーライト表示等）を「描画完了の目印」として使うこと。**
- **テスト**：`test_e2e_purchase_highlight.py`。LocalStorageへ`nseb_purchased_posts`を直接注入したブラウザコンテキスト（ケースA）で(1)詳細ページの🛒が赤色ハイライト+「1人が購入」、(2)一覧カードの🛒も赤色ハイライト+カウント1、を確認し、続けて何もフラグを持たない通常のブラウザコンテキスト（ケースB＝対照実験）で赤色ハイライトが**付かない**ことを確認する。フォールスポジティブ検知には、ケースBのように「対象記事を訪れていない別ブラウザ」との比較を必ず組み込むこと（自分が注入したフラグが効いているかだけでなく、注入していない場合に誤って効かないかも同じテストで見ないと、本セッションで実際に起きたような誤検知を検出できない）。

## 15. 「購入済み」検知が本物の購入者では一度も発火しなかった件（2026-08-14 追記：フォールスポジティブ修正の直後に、今度はフォールスネガティブが発覚）
- **不具合の経緯**：14.項でフォールスポジティブ（匿名の未購入者が誤って赤くなる）を修正した直後、今度は逆に「実際に購入した本人（Codocの売上/オーディエンス記録に残る実在の購入者、ニックネーム『忍者トゥルーサー』）のブラウザでも一切赤くならない」という報告を受けた。**自分では本物の購入者アカウントでログインしてロック解除状態のDOMを再現できない**（別人のCodocアカウントの認証情報を持っていないため）ため、最初はキャッシュ問題（ConoHa WING/nginxの古いHTML配信、12.項参照）を疑って調査したが再現せず、最終的にユーザー本人に実際の購入者ブラウザでDevToolsから該当DOM（`.wp-block-codoc-codoc-block`のouterHTML）を取得してもらい、その実機HTMLの提供を受けて原因を特定した。
- **根本原因**：ロック解除判定のロジックが「`.codoc-buy-wrap`（購入ボタン）がDOMから存在しなくなる」ことを条件にしていたが、実機HTMLで判明した実際の挙動は「要素自体は残ったまま、インライン`style="display: none"`で非表示にされるだけ」だった。存在チェック（`!container.querySelector('.codoc-buy-wrap')`）はこの非表示要素を「存在する＝購入ボタンあり＝未購入」と誤判定し続けており、**実際の購入者であっても構造的に一度も赤くならない**、14.項の修正よりさらに致命的な不具合だった。
- **恒久対策（実装済み・v2.4.1）**：`checkCodocPurchaseState`の判定条件を、実機HTMLで確認できた3つの独立したシグナルのいずれか1つで満たされれば購入済みとする方式に変更した：(1) `.codoc-buy-wrap`が要素として存在しない、または`style.display`/`getComputedStyle().display`が`'none'`（＝**非表示チェックに変更**、存在チェックのままにしない）、(2) 購入者ニックネームを表示する`.codoc-user`が存在する、(3) 本文の続きを差し込む`.codoc-entry-body-after`に空でない中身が入っている。3つとも実機HTML（ロック中は全て満たされない／ロック解除後は少なくとも(1)と(2)が満たされる）で確認済みのシグナルのみを採用しており、推測で追加した条件は無い。
- **「描画完了の目印」ガードの堅牢化**：14.項で導入した`.codoc-copyright`による「Codocウィジェットの描画が完全に終わるまで判定しない」ガードは、**ロック解除状態でも同じフッターが出るかどうか実際には未確認**（ユーザーから提供された実機HTMLの抜粋にこの要素の有無が含まれていなかったため）という前提の弱さがあった。この一点に依存して判定を永遠にスキップしてしまう（＝14.項の逆方向の新たなフォールスネガティブ）事故を防ぐため、`.codoc-copyright`が無くても「コンテナのinnerHTML文字数が500文字を超えている」場合は判定してよいというフォールバック条件を`||`で追加した（実機調査済みの初期プレースホルダは44文字、ロック中の最終状態は2500文字超だったため、500文字という閾値には十分な安全マージンがある）。**教訓：サードパーティ製ウィジェットの「描画完了の目印」を1つの要素の有無だけに依存させると、その要素が別の状態（今回で言うロック解除状態）では出ない可能性を潰しきれない。可能なら別の性質の指標（今回はHTML文字数）を`OR`で組み合わせ、単一の前提が外れても機能が完全に止まらないようにすること。**
- **なぜ実機テストだけで検出できなかったか**：14.項の対応時、匿名ブラウザでのフォールスポジティブは実機のPlaywrightテストで再現・検出できたが、今回のフォールスネガティブ（本物の購入者で発火しない）は**そもそも本物の購入者のセッションを自動テストの中で再現する手段が無かった**ため検出できなかった。この種の「自分ではログインできない外部アカウントの状態」に依存する検知ロジックを実装・検証する際は、実装者自身の検証だけで「動くはず」と判断せず、可能な限り早い段階でユーザー（実際にそのアカウントへアクセスできる人）に実機確認を依頼すること。あわせて、一度でも実機HTMLの提供を受けられたら、その具体的なDOM構造を`test_e2e_purchase_unlock_detection.py`のように**そのままテストのフィクスチャとして固定化**し、以後は同じ構造への回帰を自動テストで検出できるようにしておくこと（本物のアカウントへ毎回アクセスできるとは限らないため、一度手に入れた実機の証拠は使い回せる形で残す）。

## 16. トップページに絞り込み対象外のカテゴリー（TweetTV過去記事等）が混入していた件（2026-08-14 追記）
- **不具合の経緯**：「新規（LocalStorage/Cookieが空の）訪問者がトップページを開くと、note・エキサイトブログ以外のカテゴリーの記事（TweetTVの過去記事等）が混ざって表示される」という報告を受けて調査した。
- **根本原因（回帰ではなく機能欠落）**：`custom-search-filter.php`のカテゴリー絞り込み（`filter_search_query`、`DEFAULT_CHECKED = note + exblog`）は、**WordPressの検索結果ページ（`is_search()`）にしか適用されておらず、トップページ自体のメインクエリ（`is_home()`、Cocoonテーマの標準「最新の投稿」表示）は一度も絞り込まれていなかった**。サイドバーの絞り込みフォーム（`.custom-search-filter-form`）自体は正しくnote/exblogがデフォルトでチェックされた状態で描画されており、そのJS（別途wp-adminのCustom HTMLウィジェットに直書きされておりこのリポジトリには存在しない。`searchFilterCatsV1`というlocalStorageキーで検索結果ページとの選択状態を同期する役割のみを持つ）も正常に動作していた。しかし**フォームは「送信されて初めて」検索結果ページへ遷移し、そこで初めて絞り込みが効く**設計だったため、フォームに一切触れずトップページを直接開いただけの新規訪問者には絞り込みが一切効かず、全2844件中の未絞り込み一覧（note+exblogの666件だけでなく残り2178件も含む）がそのまま見えていた。コードベース全体を調査したが`is_home()`/`is_front_page()`を条件にした`pre_get_posts`フックは他に存在せず、「以前は絞られていたのに壊れた」回帰ではなく最初から未実装だったことを確認した。
- **恒久対策（実装済み・custom-search-filter.php v1.2.0）**：`filter_front_page_query()`を新設し、`is_home() && is_main_query()`の場合に無条件で`category__in`をDEFAULT_CHECKED（note・exblog）へ固定するようにした。トップページはサーバーサイドの初回レンダリングでありLocalStorage/Cookieを一切参照できないため、クライアント側の選択状態に依存させず**常にnote・exblogへ固定**する設計にした（他カテゴリーを見たい場合はサイドバーの絞り込みフォームで明示的に検索する導線を使う、という既存のUXをそのまま踏襲）。ページネーションを深く辿っても他カテゴリーが混入しないことを本番データ（note+exblog合計666件、67ページ+端数で丁度666件に一致、68ページ目は404）で確認済み。既存の検索結果ページ側の絞り込み（`filter_search_query`）とは別のフックとして独立して追加しており、双方は`is_home()`/`is_search()`で排他的にしか発火しないため干渉しない（両方の経路が正常に動作することを実機で確認済み）。
- **テスト**：`test_e2e_front_page_default_filter.py`。`browser.new_context()`（Cookie/LocalStorageが空の新規コンテキスト）でトップページを開き、(1) 表示された全記事IDをWP REST APIで裏取りしてnote/exblog以外のカテゴリーが1件も混ざっていないこと、(2) サイドバーの絞り込みフォームのチェックボックスがnote/exblogのみデフォルトでチェックされて描画されていること、(3) その初期状態がLocalStorage（`searchFilterCatsV1`）にも正しく同期されること、の3点をアサートする。**教訓：「サイドバーのUIコンポーネント（フォーム・チェックボックス）が正しいデフォルト状態で描画されている」ことと「実際にそのデフォルトがコンテンツ一覧へ適用されている」ことは別の話であり、UI側だけを見て安心しないこと。** UIの初期状態確認と、実際に表示される一覧データそのものの検証（API等での裏取り）を必ずセットで行うこと。

